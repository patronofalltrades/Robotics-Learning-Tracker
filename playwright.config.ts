import { defineConfig, devices } from "@playwright/test";
const mobileWidths = [320, 375, 414, 768].map((width) => ({ name: `viewport-${width}`, use: { ...devices["Desktop Chrome"], viewport: { width, height: 900 }, isMobile: width < 768 } }));
export default defineConfig({ testDir: "./e2e", timeout: 120_000, webServer: { command: "npm run dev -- -p 3010", url: "http://localhost:3010/login", reuseExistingServer: true, env: { NEXT_PUBLIC_DEMO_MODE: "true" } }, use: { baseURL: "http://localhost:3010", trace: "on-first-retry" }, projects: [{ name: "chromium", use: { ...devices["Desktop Chrome"] } }, ...mobileWidths] });
