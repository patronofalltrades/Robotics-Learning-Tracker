"use client";
import { ExternalLink } from "lucide-react";
import { resources } from "../../../lib/curriculum";
export default function ResourcesPage() { const groups = ["course", "video", "book", "paper", "docs", "repository", "essay", "vault"] as const; return <><section className="page-intro"><p className="eyebrow">Resource index</p><h1 className="display">Read close. Browse less.</h1><p className="lede">The complete resource spine from the syllabus, grouped by the kind of attention it asks for.</p></section>{groups.map((kind) => { const items = resources.filter((resource) => resource.kind === kind); if (!items.length) return null; return <section className="section" key={kind}><div className="section-head"><p className="eyebrow">{kind}</p><h2>{kind === "vault" ? "Vault-derived signals" : kind === "docs" ? "Working documentation" : kind === "video" ? "Princeton video" : "Primary spine"}</h2></div><div className="resource-list">{items.map((item) => item.url ? <a key={item.id} className="resource-item" href={item.url} target="_blank" rel="noreferrer"><span className="resource-title"><strong>{item.title}</strong><span>{item.source}</span></span><ExternalLink size={15} /></a> : <div key={item.id} className="resource-item"><span className="resource-title"><strong>{item.title}</strong><span>{item.source}</span></span></div>)}</div></section>; })}</>; }
